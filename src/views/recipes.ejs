<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes - Flavor Archive</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="navbar-brand">
                <span class="logo-icon">🍴</span>
                <h1><span class="orange-text" style="color: #ff9f43;">Flavor</span><span class="green-text">Archive</span></h1>
            </a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/recipes" class="active">Recipes</a></li>
                <li><a href="/recipes/add">Add Recipe</a></li>
                <li><a href="/blog">Blog</a></li>
            </ul>
            <div class="nav-actions">
                <form class="search-form" action="/search" method="GET">
                    <input type="text" name="q" placeholder="Search recipes..." class="search-input">
                    <button type="submit" class="search-btn">🔍 Search</button>
                </form>
                <button class="account-btn">👤 Account</button>
            </div>
        </div>
    </nav>

    <section style="height: 380px; background: url('https://images.unsplash.com/photo-1495521821757-a1efb6729352?w=1600&h=500&fit=crop') center/cover no-repeat; position: relative; display: flex; align-items: center; justify-content: center;">
        <div style="position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0, 0, 0, 0.45);"></div>
        <div class="container" style="position: relative; z-index: 2; text-align: center;">
            <h2 style="font-size: 48px; font-weight: 800; color: white; margin-bottom: 12px; text-shadow: 0 3px 10px rgba(0,0,0,0.4); line-height: 1.2;">👨‍🍳 All Recipes</h2>
            <p style="font-size: 18px; color: rgba(255,255,255,0.95); text-shadow: 0 2px 6px rgba(0,0,0,0.4); font-weight: 300;">Browse our collection of amazing recipes from around the world</p>
        </div>
    </section>

    <main>
        <div class="container">
            <div style="display: flex; justify-content: space-between; align-items: center; margin: 40px 0 30px 0;">
                <h3 style="margin: 0; font-size: 20px; color: var(--text-dark);">Filter Recipes</h3>
                <a href="/recipes/add" class="btn btn-primary">➕ Add New Recipe</a>
            </div>

            <!-- Filter Section -->
            <div style="background: white; padding: 20px; border-radius: 8px; margin-bottom: 30px; box-shadow: var(--shadow);">
                <form style="display: flex; gap: 20px; flex-wrap: wrap; align-items: flex-end;">
                    <div class="form-group" style="margin: 0;">
                        <label for="mealType" style="display: block; font-size: 13px; font-weight: 600; margin-bottom: 5px; color: var(--text-muted);">🍽️ Meal Type</label>
                        <select id="mealType" name="mealType" onchange="this.form.submit()" style="padding: 10px; border: 1px solid var(--border); border-radius: 4px; font-size: 13px; cursor: pointer;">
                            <option value="All">All Meals</option>
                            <option value="Breakfast" <%= filters?.mealType === 'Breakfast' ? 'selected' : '' %>>🌅 Breakfast</option>
                            <option value="Lunch" <%= filters?.mealType === 'Lunch' ? 'selected' : '' %>>🍽️ Lunch</option>
                            <option value="Dinner" <%= filters?.mealType === 'Dinner' ? 'selected' : '' %>>🌙 Dinner</option>
                            <option value="Dessert" <%= filters?.mealType === 'Dessert' ? 'selected' : '' %>>🍰 Dessert</option>
                        </select>
                    </div>
                    <div class="form-group" style="margin: 0;">
                        <label for="difficulty" style="display: block; font-size: 13px; font-weight: 600; margin-bottom: 5px; color: var(--text-muted);">📊 Difficulty</label>
                        <select id="difficulty" name="difficulty" onchange="this.form.submit()" style="padding: 10px; border: 1px solid var(--border); border-radius: 4px; font-size: 13px; cursor: pointer;">
                            <option value="All">All Levels</option>
                            <option value="Easy" <%= filters?.difficulty === 'Easy' ? 'selected' : '' %>>🟢 Easy</option>
                            <option value="Medium" <%= filters?.difficulty === 'Medium' ? 'selected' : '' %>>🟡 Medium</option>
                            <option value="Hard" <%= filters?.difficulty === 'Hard' ? 'selected' : '' %>>🔴 Hard</option>
                        </select>
                    </div>
                    <div class="form-group" style="margin: 0;">
                        <label for="minRating" style="display: block; font-size: 13px; font-weight: 600; margin-bottom: 5px; color: var(--text-muted);">⭐ Min Rating</label>
                        <select id="minRating" name="minRating" onchange="this.form.submit()" style="padding: 10px; border: 1px solid var(--border); border-radius: 4px; font-size: 13px; cursor: pointer;">
                            <option value="">All Ratings</option>
                            <option value="3" <%= filters?.minRating === '3' ? 'selected' : '' %>>3+ Stars</option>
                            <option value="4" <%= filters?.minRating === '4' ? 'selected' : '' %>>4+ Stars</option>
                            <option value="5" <%= filters?.minRating === '5' ? 'selected' : '' %>>5 Stars</option>
                        </select>
                    </div>
                </form>
            </div>

            <% if (recipes && recipes.length > 0) { %>
                <div class="recipe-grid">
                    <% recipes.forEach(recipe => { %>
                        <div class="recipe-card">
                            <div class="recipe-card-image-wrapper">
                                <img src="<%= recipe.image %>" alt="<%= recipe.name %>" class="recipe-card-image">
                                <div class="recipe-badges">
                                    <span class="badge badge-mealtype"><%= recipe.mealType %></span>
                                    <span class="badge badge-difficulty"><%= recipe.difficulty %></span>
                                </div>
                            </div>
                            <div class="recipe-card-content">
                                <div>
                                    <h3><%= recipe.name %></h3>
                                    <p class="cuisine-type"><%= recipe.cuisine %></p>
                                </div>
                                
                                <div class="rating-section">
                                    <div class="stars">
                                        <% for(let i = 1; i <= 5; i++) { %>
                                            <span class="star <%= parseFloat(recipe.averageRating) >= i ? 'active' : '' %>">★</span>
                                        <% } %>
                                    </div>
                                    <span class="rating-count"><%= parseFloat(recipe.averageRating).toFixed(1) %> (<%= recipe.ratings ? recipe.ratings.length : 0 %>)</span>
                                </div>

                                <div class="recipe-meta-info">
                                    <span>⏱️ <%= recipe.cookTime %></span>
                                    <span>👥 <%= recipe.servings %> servings</span>
                                </div>

                                <div class="recipe-card-actions">
                                    <a href="/recipes/<%= recipe._id %>" class="btn btn-primary btn-sm">View</a>
                                    <a href="/recipes/edit/<%= recipe._id %>" class="btn btn-secondary btn-sm">Edit</a>
                                    <form method="POST" action="/recipes/delete/<%= recipe._id %>" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this recipe?');">
                                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div style="background: white; padding: 80px 20px; border-radius: 8px; text-align: center;">
                    <h3 style="font-size: 24px; color: var(--text-dark); margin-bottom: 10px;">👨‍🍳 No recipes found</h3>
                    <p style="color: var(--text-muted); margin-bottom: 20px;">No recipes match your filters. Try adjusting your search criteria.</p>
                    <a href="/recipes/add" class="btn btn-primary">➕ Add the First Recipe!</a>
                </div>
            <% } %>
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 Flavor Archive. All rights reserved. | Made with ❤️ for food lovers</p>
        </div>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>