<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Flavor Archive</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="navbar-brand">
                <span class="logo-icon">🍴</span>
                <h1><span class="orange-text" style="color: #ff9f43;">Flavor</span><span class="green-text">Archive</span></h1>
            </a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/recipes">Recipes</a></li>
                <li><a href="/recipes/add">Add Recipe</a></li>
                <li><a href="/blog" class="active">Blog</a></li>
            </ul>
            <div class="nav-actions">
                <form class="search-form" action="/search" method="GET">
                    <input type="text" name="q" placeholder="Search recipes..." class="search-input">
                    <button type="submit" class="search-btn">🔍 Search</button>
                </form>
                <button class="account-btn">👤 Account</button>
            </div>
        </div>
    </nav>

    <main>
        <div class="container">
            <a href="/blog" class="back-link" style="display: inline-block; margin-bottom: 30px; color: var(--primary-green); font-weight: 600; text-decoration: none;">← Back to Blog</a>

            <article style="background: white; border-radius: 12px; overflow: hidden; box-shadow: var(--shadow-lg); padding: 40px;">
                <!-- Header -->
                <div style="margin-bottom: 30px;">
                    <h1 style="font-size: 40px; margin-bottom: 15px; color: var(--text-dark); line-height: 1.3;"><%= post.title %></h1>
                    <div style="display: flex; gap: 20px; align-items: center; flex-wrap: wrap; padding-bottom: 20px; border-bottom: 2px solid var(--border);">
                        <div style="display: flex; gap: 10px; font-size: 13px; color: var(--text-muted);">
                            <span>✍️ By <strong style="color: var(--primary-green);"><%= post.author %></strong></span>
                        </div>
                        <div style="font-size: 13px; color: var(--text-muted);">
                            📅 <%= new Date(post.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                        </div>
                    </div>
                </div>

                <!-- Featured Image -->
                <img src="<%= post.image %>" alt="<%= post.title %>" style="width: 100%; height: 400px; object-fit: cover; border-radius: 8px; margin-bottom: 30px;">

                <!-- Content -->
                <div style="font-size: 16px; line-height: 1.8; color: var(--text-secondary); margin-bottom: 30px;">
                    <%= post.content %>
                </div>

                <!-- Tags -->
                <% if (post.tags && post.tags.length > 0) { %>
                    <div style="background: var(--light-green); padding: 20px; border-radius: 8px; margin-bottom: 30px;">
                        <h3 style="margin: 0 0 12px 0; color: var(--text-dark); font-size: 14px;">📌 Tags</h3>
                        <div style="display: flex; flex-wrap: wrap; gap: 8px;">
                            <% post.tags.forEach(tag => { %>
                                <span style="background: white; border: 1px solid var(--primary-green); color: var(--primary-green); padding: 6px 12px; border-radius: 12px; font-size: 12px; font-weight: 600;">📌 <%= tag %></span>
                            <% }); %>
                        </div>
                    </div>
                <% } %>

                <!-- Actions -->
                <div style="display: flex; justify-content: space-between; align-items: center; padding-top: 20px; border-top: 2px solid var(--border);">
                    <a href="/blog" class="btn btn-secondary">← Back to Blog</a>
                    <a href="/recipes" class="btn btn-primary">Explore Recipes 🍴</a>
                </div>
            </article>
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 Flavor Archive. All rights reserved. | Made with ❤️ for food lovers</p>
        </div>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>
