<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - Flavor Archive</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="navbar-brand">
                <span class="logo-icon">🍴</span>
                <h1><span class="orange-text" style="color: #ff9f43;">Flavor</span><span class="green-text">Archive</span></h1>
            </a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/recipes">Recipes</a></li>
                <li><a href="/recipes/add">Add Recipe</a></li>
                <li><a href="/blog">Blog</a></li>
            </ul>
            <div class="nav-actions">
                <form class="search-form" action="/search" method="GET">
                    <input type="text" name="q" placeholder="Search recipes..." class="search-input" value="<%= query %>">
                    <button type="submit" class="search-btn">🔍 Search</button>
                </form>
                <button class="account-btn">👤 Account</button>
            </div>
        </div>
    </nav>

    <section style="height: 380px; background: url('https://images.unsplash.com/photo-1495521821757-a1efb6729352?w=1600&h=500&fit=crop') center/cover no-repeat; position: relative; display: flex; align-items: center; justify-content: center;">
        <div style="position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0, 0, 0, 0.45);"></div>
        <div class="container" style="position: relative; z-index: 2; text-align: center;">
            <h2 style="font-size: 48px; font-weight: 800; color: white; margin-bottom: 12px; text-shadow: 0 3px 10px rgba(0,0,0,0.4); line-height: 1.2;">🔍 Search Results</h2>
            <p style="font-size: 18px; color: rgba(255,255,255,0.95); text-shadow: 0 2px 6px rgba(0,0,0,0.4); font-weight: 300;">Showing results for: <strong style="color: #ffeb3b;">"<%= query %>"</strong></p>
        </div>
    </section>

    <main>
        <div class="container">
            <% if (recipes && recipes.length > 0) { %>
                <div style="background: var(--light-green); padding: 16px; border-radius: 8px; margin-bottom: 30px; border-left: 4px solid var(--primary-green);">
                    <p style="margin: 0; color: var(--text-dark); font-weight: 500;">Found <strong style="color: var(--primary-green); font-size: 16px;"><%= recipes.length %></strong> recipe<%= recipes.length !== 1 ? 's' : '' %> for \"<strong><%= query %></strong>\"</p>
                </div>
                <div class="recipe-grid">
                    <% recipes.forEach(recipe => { %>
                        <div class="recipe-card">
                            <div class="recipe-card-image-wrapper">
                                <img src="<%= recipe.image %>" alt="<%= recipe.name %>" class="recipe-card-image">
                                <div class="recipe-badges">
                                    <span class="badge badge-mealtype"><%= recipe.mealType %></span>
                                    <span class="badge badge-difficulty"><%= recipe.difficulty %></span>
                                </div>
                            </div>
                            <div class="recipe-card-content">
                                <div>
                                    <h3><%= recipe.name %></h3>
                                    <p class="cuisine-type"><%= recipe.cuisine %></p>
                                </div>
                                
                                <div class="rating-section">
                                    <div class="stars">
                                        <% for(let i = 1; i <= 5; i++) { %>
                                            <span class="star <%= parseFloat(recipe.averageRating) >= i ? 'active' : '' %>">★</span>
                                        <% } %>
                                    </div>
                                    <span class="rating-count"><%= parseFloat(recipe.averageRating).toFixed(1) %> (<%= recipe.ratings ? recipe.ratings.length : 0 %>)</span>
                                </div>

                                <div class="recipe-meta-info">
                                    <span>⏱️ <%= recipe.cookTime %></span>
                                    <span>👥 <%= recipe.servings %> servings</span>
                                </div>

                                <div class="recipe-card-actions">
                                    <a href="/recipes/<%= recipe._id %>" class="btn btn-primary btn-sm\">View</a>
                                    <a href=\"/recipes/edit/<%= recipe._id %>\" class=\"btn btn-secondary btn-sm\">Edit</a>
                                    <form method=\"POST\" action=\"/recipes/delete/<%= recipe._id %>\" style=\"display: inline;\" onsubmit=\"return confirm('Are you sure you want to delete this recipe?');\">
                                        <button type=\"submit\" class=\"btn btn-danger btn-sm\">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div style="background: white; padding: 80px 20px; border-radius: 8px; text-align: center;">
                    <h3 style="font-size: 24px; color: var(--text-dark); margin-bottom: 10px;">🔍 No recipes found</h3>
                    <p style="color: var(--text-muted); margin-bottom: 20px;">No recipes match your search for "<strong><%= query %></strong>". Try different keywords or browse our collection.</p>
                    <div style="display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;">
                        <a href="/recipes" class="btn btn-primary">Browse All Recipes</a>
                        <a href="/" class="btn btn-secondary">Back to Home</a>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 Flavor Archive. All rights reserved. | Made with ❤️ for food lovers</p>
        </div>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>